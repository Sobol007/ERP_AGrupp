#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура БыстрыйПоиск(Команда)
	КлючЗаписи = сшпОбщегоНазначения.ПолучитьКлючЗаписиПоСтрокеПоиска("сшпОчередьОтправляемыхСистемныхСообщений", СтрокаБыстрогоПоиска);
	Если ТипЗнч(КлючЗаписи) = Тип("Строка") Тогда 
		ПоказатьОповещениеПользователя(КлючЗаписи);
	Иначе
		Элементы.Список.ТекущаяСтрока = КлючЗаписи;
	КонецЕсли;
КонецПроцедуры

#КонецОбласти
